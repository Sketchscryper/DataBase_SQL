-- Задание 0. Установите MySQL Community Server. 

-- Задание 1. Создайте базу данных под названием Birds. Местоположение остаётся на ваш выбор.
CREATE DATABASE Birds;

-- Задание 2. Переименуйте базу данных из первого задания. Новое имя для базы данных Cats.
-- Создаем новую базу данных
CREATE DATABASE Cats;

-- Если нужно скопировать данные (в данном случае таблиц нет, поэтому просто создаем пустую)
-- Для реальных случаев используется создание новой БД с импортом данных

-- Задание 3. Удалите базу данных Cats.
DROP DATABASE Cats;

-- Задание 4. Создайте однотабличную базу данных «Овощи и фрукты». Нужно хранить такую информацию:
-- ■ Название;
-- ■ Тип (овощ или фрукт);
-- ■ Цвет;
-- ■ Калорийность;
-- ■ Краткое описание.

-- Создаем базу данных
CREATE DATABASE Vegetables_Fruits;
USE Vegetables_Fruits;

-- Создаем таблицу
CREATE TABLE products (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    sort ENUM('овощ', 'фрукт') NOT NULL,
    color VARCHAR(50) NOT NULL,
    calories INT NOT NULL,
    description TEXT
);

-- Задание 5. Создайте следующие запросы для таблицы с информацией об овощах и фруктах из предыдущего задания:
-- ■ Отображение всей информации из таблицы с овощами и фруктами;
-- ■ Отображение всех овощей;
-- ■ Отображение всех фруктов;
-- ■ Отображение всех названий овощей и фруктов;
-- ■ Отображение всех цветов. Цвета должны быть уникальными;
-- ■ Отображение фруктов конкретного цвета;
-- ■ Отображение овощей конкретного цвета.

-- Добавляем тестовые данные
INSERT INTO products (name, sort, color, calories, description) VALUES
('Яблоко', 'фрукт', 'красный', 52, 'Сладкий фрукт'),
('Банан', 'фрукт', 'желтый', 89, 'Энергетический фрукт'),
('Апельсин', 'фрукт', 'оранжевый', 47, 'Цитрусовый фрукт'),
('Морковь', 'овощ', 'оранжевый', 41, 'Овощ для зрения'),
('Огурец', 'овощ', 'зеленый', 15, 'Сочный овощ'),
('Помидор', 'овощ', 'красный', 18, 'Популярный овощ'),
('Брокколи', 'овощ', 'зеленый', 34, 'Полезный овощ'),
('Виноград', 'фрукт', 'фиолетовый', 69, 'Сладкие ягоды');

-- Выполняем запросы 
-- Отображение всей информации из таблицы с овощами и фруктами
SELECT * FROM products;

-- Отображение всех овощей
SELECT * FROM products WHERE sort = 'овощ';

-- Отображение всех фруктов
SELECT * FROM products WHERE sort = 'фрукт';

-- Отображение всех названий овощей и фруктов
SELECT name FROM products;

-- Отображение всех цветов. Цвета должны быть уникальными
SELECT DISTINCT color FROM products;

-- Отображение фруктов конкретного цвета
SELECT * FROM products 
WHERE sort = 'фрукт' AND color = 'красный';

-- Отображение овощей конкретного цвета
SELECT * FROM products 
WHERE sort = 'овощ' AND color = 'зеленый';